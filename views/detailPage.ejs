<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Detail Page</h1>
    <h2><%= data.title %></h2>
    <p><%= data.content %></p>
    
    <% errorMessage.forEach((item)=>{ %>
        <p style="color: red;"><%= item %></p>
    <% }) %>
    
    <table border="1">
        <tr>
            <td>Comments</td>
            <td>Nama | username</td>
        </tr>

        <% data.Comments.forEach((item)=>{ %>
        <tr>
            <td><%= item.content %></td>
            <td><%= item.User.Profile.fullName %>@<%= item.User.username %></td>
        </tr>
        <% }) %>
        
    </table>
    <form action="/comment/add/<%= data.id %>/<%= userId %>" method="post">
        <input name="content" id=""></input><br>
        <a href=""><button>Comment</button></a>
    </form>
</body>
</html>