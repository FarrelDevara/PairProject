<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Edit News Form</h1>
    <form action="/berita/edit/<%= data.id %>" method="post">

        <label for="">Title</label><br>
        <input type="text" name="title" value="<%= data.title %>" style="width: 1000px; "><br><br>

        <label for="">Content</label><br>
        <textarea type="textarea" name="content" value="" style="width: 1000px; "><%= data.content %></textarea><br><br>

        <label for="">imageUrl</label><br>
        <input type="text" name="imageUrl" value="<%= data.imageUrl %>" style="width: 1000px; "><br><br>

        <select name="CategoryId" id="">
            <option value="">Choose..</option>
            <% dataCategory.forEach((item)=>{ %>
                <option value="<%= item.id %>" <%= item.id === data.CategoryId ? 'selected' : "" %>><%= item.categoryName %></option>
            <% }) %>
        </select>
        <input type="submit" value="Add">
    </form>
</body>
</html>